# UI Bug & UX Checklist

Контекст проверки:
- Первичная проверка: 2026-06-23.
- Повторная проверка после исправлений: 2026-06-23.
- Локальный web: `http://127.0.0.1:5173`.
- API: `http://localhost:3000`.
- Авторизация: `megamanok99@gmail.com / testtest`.
- Основной viewport проверки: mobile `390x844`, как Telegram WebApp.

Легенда:
- `[x]` - проверено и исправлено.
- `[ ]` - осталось исправить.

## Повторная Проверка

- [x] `/friends`: табы больше не обрезаются, horizontal overflow ушел.
- [x] `/workouts`: карточки категорий больше не выезжают за viewport.
- [x] `/weight`: horizontal overflow ушел, поле кг больше не схлопывается.
- [x] `/products`: появился начальный empty state.
- [x] `/products`: поиск `ряженка` теперь возвращает только ряженку.
- [x] `/profile`: select-поля стали читаемыми, контраст исправлен.
- [x] `Loader.tsx`: SVG props уже camelCase, старые React warnings больше не актуальны для текущего исходника.
- [ ] `npm run build` все еще падает на TypeScript unused-переменных.
- [ ] `/today`: остались маленькие пустые edit/delete-кнопки без `aria-label`.
- [ ] `/today` и `/reports`: стрелки даты без `aria-label`, touch target ниже желательного размера.
- [ ] Footer/BurgerMenu все еще сделаны через clickable `div`, без полноценной семантики навигации.
- [ ] Местами текст визуально слипается: `Текущий42кг`, `История веса3дней`, `Ряженка 1%К: ...`.

## P0 / P1: Сломанная Верстка И Логика

- [x] **Починить горизонтальный overflow на странице веса `/weight`.**
  - Было: страница шире экрана (`documentWidth` около `447px` при viewport `390px`), верхние карточки статистики уезжали вправо, форма "Записать вес" сжимала поле кг почти до нуля.
  - Повторная проверка: `docW/bodyW` равны `390px`, форма читаемая, поле кг нормальной ширины.
  - Где смотреть: `apps/web/src/pages/WeightHistoryPage.tsx`.

- [x] **Починить горизонтальный overflow на странице тренировок `/workouts`.**
  - Было: двухколоночная сетка категорий не помещалась, правые карточки обрезались.
  - Повторная проверка: overflow не найден, карточки видимы в пределах viewport.
  - Где смотреть: `apps/web/src/pages/WorkoutsPage.tsx`.

- [x] **Починить табы на странице друзей `/friends`.**
  - Было: третий таб "Подписчики" обрезался справа.
  - Повторная проверка: все три таба помещаются на `390px`.
  - Где смотреть: `apps/web/src/pages/FriendsPage.tsx`.

- [x] **Починить поиск продуктов `/products`: search игнорировался API.**
  - Было: при запросе `ряженка` список начинался с "Молоко", "Кефир", а точные совпадения были ниже.
  - Повторная проверка: `ряженка` возвращает `Ряженка 1%`, `Ряженка 2.5%`, `Ряженка 4%`.
  - Где смотреть: `apps/api/src/products/products.service.ts`.

- [x] **Добавить нормальное начальное состояние на `/products`.**
  - Было: до ввода поискового запроса экран выглядел пустым.
  - Повторная проверка: есть заголовок "Продукты" и подсказка "Начните вводить название продукта".

## P1: Сборка И Технические Блокеры

- [ ] **Починить `npm run build` в `apps/web`.**
  - Команда: `cmd /c "npm.cmd run build"`.
  - Текущий результат: сборка падает на `tsc`.
  - Ошибки:
    - `src/features/TodayComponents/DayChanger/index.tsx(7,9): 'handleDateChange' is declared but its value is never read.`
    - `src/theme/theme.ts(188,10): 'hexToRgb' is declared but its value is never read.`
    - `src/ui/DashboardRing.tsx(49,9): 'asize' is declared but its value is never read.`
  - Ожидаемо: удалить unused-переменные или начать использовать их, затем добиться зеленой сборки.

## P2: Читаемость, Интерактивность, UX

- [x] **Исправить контраст select-полей на `/profile`.**
  - Было: значения "Мужской", "Средняя", "Похудение" почти не читались.
  - Повторная проверка: фон select темный (`rgb(26, 58, 74)`), текст белый, контраст нормальный.

- [ ] **Улучшить нижнюю навигацию.**
  - Симптом: элементы footer сделаны `div onClick`, а не `button`/`nav`/`a`.
  - Симптом: центральная кнопка меню (`BurgerMenu`) сделана `div`, нет доступного имени и keyboard-семантики.
  - Где смотреть:
    - `apps/web/src/widget/Footer/Footer.tsx`
    - `apps/web/src/widget/BurgerMenu.tsx`
  - Ожидаемо: использовать `nav` + `button` или `NavLink`, добавить `aria-label`, активное состояние, keyboard focus.

- [ ] **Добавить `aria-label` для иконок-кнопок на `/today`.**
  - Повторная проверка: рядом с записью еды остались две пустые кнопки примерно `26x29px`, без текста и `aria-label`.
  - Вероятно, это edit/delete.
  - Ожидаемо:
    - `aria-label="Редактировать запись"`
    - `aria-label="Удалить запись"`
    - увеличить touch target до примерно `44x44px`.

- [ ] **Добавить понятные подписи для стрелок даты.**
  - Повторная проверка: кнопки `←` и `→` на `/today` и `/reports` все еще без `aria-label`.
  - Дополнительно: высота около `34px`, меньше желательного мобильного touch target.
  - Где смотреть:
    - `apps/web/src/features/TodayComponents/DayChanger/index.tsx`
    - `apps/web/src/pages/ReportsPage.tsx`
  - Ожидаемо:
    - `aria-label="Предыдущий день"` / `aria-label="Следующий день"` для `/today`.
    - `aria-label="Предыдущий период"` / `aria-label="Следующий период"` или неделя/месяц для `/reports`.
    - touch target около `44x44px`.

- [ ] **Развести слипшийся текст в карточках и списках.**
  - Новое после повторной проверки: layout больше не ломается, но текст местами выглядит склеенным.
  - Примеры:
    - `/weight`: `Текущий42кг`, `История веса3дней`.
    - `/products`: `Ряженка 1%К: 40 | ...`.
    - `/today`: `Серия2 дней`, `XP за неделю50`, `Съедено54`.
  - Ожидаемо: добавить пробелы/отступы/раздельные inline-блоки, чтобы значения не слипались с label.

- [ ] **Сделать feedback при невалидном email на логине.**
  - Симптом: если ввести `testtest / testtest`, browser native validation блокирует submit из-за `type="email"`, но пользователь не видит кастомного сообщения приложения.
  - Ожидаемо: явное сообщение "Введите email в формате name@example.com" или встроенная валидация с подсветкой поля.

- [ ] **Локализовать серверные ошибки авторизации.**
  - Симптом: при неверных данных показывается `Invalid credentials`.
  - Ожидаемо: русское сообщение, например "Неверный email или пароль".

- [ ] **Добавить заголовки/контекст на пустые или короткие экраны.**
  - `/products` частично закрыто: заголовок и начальная подсказка появились.
  - Осталось проверить/улучшить `/feed`: сейчас короткое "Нет событий" без полезного объяснения.
  - Ожидаемо: короткий заголовок экрана и полезный empty state.

- [ ] **Проверить нижний padding контента под fixed footer.**
  - На `/today` нижняя часть списка и footer все еще визуально близко.
  - Ожидаемо: общий layout должен добавлять безопасный нижний отступ с учетом footer и Telegram safe area.

- [x] **Улучшить карточки тренировок на мобильном: убрать явное слипание названия и описания.**
  - Было: `ГрудьВерхняя часть...`, `СпинаШирочайшие...`.
  - Повторная проверка: название и описание визуально разделены, overflow нет.
  - Остаточный UX-риск: длинные описания все еще мелкие, но это уже полировка.

- [ ] **Улучшить визуал графика веса.**
  - Симптом: график на `/weight` выглядит как несколько больших прямоугольников без понятной шкалы.
  - Повторная проверка: overflow ушел, но информативность графика все еще слабая.
  - Ожидаемо: понятная подпись осей/точек, аккуратная шкала, нормальная работа при малом количестве точек.

- [ ] **Добавить подтверждение на destructive actions.**
  - Симптом: кнопки удаления записей/шаблонов выглядят как маленький `X` или пустая иконка; неясно, есть ли подтверждение.
  - Ожидаемо: confirmation sheet/dialog перед удалением и понятная подпись действия.

## P2 / P3: Accessibility И Семантика

- [ ] **Сделать onboarding indicators недекоративными или скрыть от accessibility tree.**
  - Симптом первичной проверки: точки/индикаторы слайдов определялись как кнопки без названия.
  - Ожидаемо: если кликабельны - `aria-label="Слайд 1"`; если декоративны - не button и `aria-hidden`.
  - Статус: после повторной проверки не перепроверялось, оставить открытым.

- [ ] **Исправить footer как навигационный landmark.**
  - Повторная проверка: footer все еще на clickable `div`.
  - Ожидаемо: `nav` с понятными ссылками/кнопками, keyboard navigation, focus styles.

- [ ] **Проверить размеры touch targets.**
  - Повторная проверка:
    - `/today`: edit/delete кнопки около `26x29px`.
    - `/today` и `/reports`: стрелки высотой около `34px`.
    - `/friends`: табы высотой около `35px`; не критично, но ниже рекомендованных `44px`.
  - Ожидаемо: минимум около `44x44px` для основных touch actions.

- [ ] **Проверить focus states.**
  - Симптом: у кастомных clickable `div` нет нормального focus state.
  - Ожидаемо: видимый focus для keyboard users.

## Технические Warnings / Качество

- [x] **Исправить React warnings в `Loader`.**
  - Было:
    - `Invalid DOM property stroke-opacity. Did you mean strokeOpacity?`
    - `Invalid DOM property stroke-width. Did you mean strokeWidth?`
    - `Invalid DOM property stroke-linecap. Did you mean strokeLinecap?`
  - Повторная проверка исходника и Vite-модуля: props уже `strokeOpacity`, `strokeWidth`, `strokeLinecap`.
  - Примечание: в старой вкладке могли оставаться накопленные warnings, но текущий код исправлен.

- [ ] **Проверить дубли Mongoose индексов.**
  - Симптомы в API логах:
    - duplicate schema index on `{"nameNormalized":1}`
    - duplicate schema index on `{"sessionId":1}`
  - Ожидаемо: убрать дублирующее объявление `index: true` или `schema.index()`.

- [ ] **Убрать `console.log` из пользовательских действий.**
  - Первичный симптом: в `ProductsPage` клик по продукту писал `Selected product ID`, но не делал пользовательски полезного действия.
  - Статус: после повторной проверки отдельно не подтверждалось, оставить открытым до проверки кода/клика.
  - Ожидаемо: либо открыть карточку/добавление продукта, либо убрать интерактивность/лог.

## UX-Улучшения Сверх Багов

- [ ] **Сделать продукты реально полезными как справочник.**
  - Частично закрыто: search фильтрует релевантно, empty state появился.
  - Осталось:
    - показать популярные/недавние продукты до поиска;
    - добавить состояние "ничего не найдено" с предложением создать продукт;
    - подсвечивать совпавшую часть названия;
    - сортировать результаты по релевантности: exact > startsWith > contains.

- [ ] **Унифицировать заголовки экранов.**
  - Сейчас часть экранов начинается без заголовка или с неявного контекста.
  - Нужна единая схема: title, optional subtitle, actions.

- [ ] **Унифицировать segmented controls.**
  - `/friends`, `/league`, `/workouts`, `/reports` используют похожие табы, но адаптивность и размеры разные.
  - Лучше вынести общий компонент segmented control с overflow/wrap правилами и touch target не ниже `44px`.

- [ ] **Улучшить микро-копирайт empty states.**
  - `Нет данных` и `Нет событий` лучше заменить на более конкретные сообщения: что это значит и что можно сделать дальше.
  - `/products` уже лучше, но остальные пустые состояния стоит пройти отдельно.

- [ ] **Проверить все экраны на 360px ширине.**
  - На `390px` основные overflow закрыты.
  - Минимум проверить: `/today`, `/friends`, `/workouts`, `/weight`, `/entry/new`, `/profile`, `/reports`.

- [ ] **Добавить smoke UI-тесты или Playwright visual checks.**
  - Проверять отсутствие horizontal scroll: `document.documentElement.scrollWidth <= window.innerWidth`.
  - Проверять основные маршруты после логина.
  - Проверять, что search products реально фильтрует результат.
  - Проверять, что интерактивные иконки имеют accessible name.

