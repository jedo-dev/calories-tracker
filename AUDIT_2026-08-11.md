# Аудит приложения — 2026-08-11

Сводка по результатам ревью кода (web + api) и прохода по приложению.
Статусы: ⬜ не начато · 🟨 в работе · ✅ сделано

---

## Блок 1 — Безопасность (критично)

| # | Статус | Проблема | Где |
|---|--------|----------|-----|
| 1.1 | ✅ | Нет глобального `ValidationPipe` — лишние поля тела запроса проходят во все `Object.assign`/spread | `apps/api/src/main.ts` |
| 1.2 | ✅ | NoSQL-инъекция в логине/регистрации: DTO без валидаторов, `{"email":{"$ne":null}}` уходит в `findOne` | `auth/auth.controller.ts` |
| 1.3 | ✅ | Нет rate-limit на `/auth/login` — открытый брутфорс | `auth/auth.controller.ts` |
| 1.4 | ✅ | Mass-assignment: в `POST /measurements` можно передать чужой `userId` в теле | `measurements/*` |
| 1.5 | ✅ | Regex-инъекция/ReDoS в поиске друзей: `new RegExp(query)` без экранирования | `friends/friends.service.ts:45` |
| 1.6 | ✅ | Whitelist аутентификации по `path.startsWith(...)` вместо `@Public()` декоратора | `common/guards/jwt-auth.guard.ts` |
| 1.7 | ✅ | CORS `origin: true` + `credentials: true` — отражается любой Origin | `main.ts` |
| 1.8 | ✅ | Поля `startWeightKg`/`targetWeightKg`/`targetDate` не описаны в DTO профиля (нет диапазонов; `targetWeightKg: 0` даёт деление на 0 в прогнозе) | `profile/dto/update-profile.dto.ts` |
| 1.9 | ✅ | Нет обработки 401 на фронте — протухший токен = молча пустые страницы | `apps/web/src/api/client.ts` |

## Блок 2 — Продуктовые баги

| # | Статус | Проблема | Где |
|---|--------|----------|-----|
| 2.1 | ✅ | XP не начисляется за тренировку (+5) и воду (+3), хотя лига это обещает; `grantXp` зовётся только из entries | `workouts/workout.service.ts` (finishSession), `water/water.service.ts`, `social/social.service.ts` |
| 2.2 | ✅ | Race condition в XP/стриках: read-modify-write вместо `$inc`; параллельные записи затирают друг друга | `social/social.service.ts:56-157` |
| 2.3 | ✅ | «Сегодня» считается по таймзоне сервера — пользователи восточнее не могут записать вес за сегодня | `weight/weight.service.ts:18` |
| 2.4 | ✅ | `finishSession` не идемпотентен: двойной тап дублирует событие ленты и пересчёт PR | `workouts/workout.service.ts` |
| 2.5 | ✅ | Автосейв подходов и finish тренировки при сбое сети падают в `console.error` без сигнала юзеру | `pages/ActiveWorkoutPage.tsx:125,249` |
| 2.6 | ✅ | Редактор рецепта теряет несохранённое при SPA-навигации назад (guard только на beforeunload) | `pages/RecipeEditorPage.tsx:462` |
| 2.7 | ✅ | Лайк рецепта создаёт событие `recipe_published` в ленте (лайк выглядит как публикация) | `recipes/recipes.service.ts:605-649` |
| 2.8 | ✅ | `apply` плана питания не атомарен и не идемпотентен: падение на середине / двойной клик = дубли записей дневника | `meal-plan/meal-plan.service.ts:473-511` |

## Блок 3 — UX

| # | Статус | Проблема | Где |
|---|--------|----------|-----|
| 3.1 | ✅ | Браузерные `confirm()`/`alert()` в 8 файлах при готовом `ConfirmSheet`; часть текстов на английском | TodayPage, ProductsPage, WeightHistoryPage, AddEntryPage, RecipeDetailPage, RecipesPage, RichTextEditor |
| 3.2 | ✅ | `ConfirmSheet` лежит в `widgets/workout/`, но используется 3 доменами — место в `ui/` | `widgets/workout/ConfirmSheet.tsx` |
| 3.3 | ✅ | Ошибки загрузки выглядят как «пусто»: вес, упражнения, поиск продуктов, MealPlan-история | ExercisesPage:109, WeightHistoryPage:301, AddEntryPage:174, MealPlanPage |
| 3.4 | ✅ | Удаление записи еды перезагружает весь день (4 запроса + полноэкранный Loader) | `pages/TodayPage.tsx` handleDeleteEntry |
| 3.5 | ✅ | Цель воды захардкожена 2000 мл — не зависит от профиля | `pages/TodayPage.tsx` |
| 3.6 | ✅ | Нет плюрализации: «1 дней», «3/3 подходов» — нужен `plural(n, ...)` | WeightHistoryPage, workout-виджеты |
| 3.7 | ✅ | `/products` — страница-сирота (нет ссылки в меню/футере) | `App.tsx:73`, `widget/Drawer.tsx` |
| 3.8 | ✅ | «Все упражнения» — мёртвая кнопка при пустых категориях | `pages/WorkoutsPage.tsx:107` |
| 3.9 | ✅ | Пустая лента без CTA «Найти друзей» | FeedPage |
| 3.10 | ✅ | MealPlanPage: нет состояния загрузки, `hasProfile` мигает | `pages/MealPlanPage.tsx` |

## Блок 4 — Консистентность кода

| # | Статус | Проблема | Где |
|---|--------|----------|-----|
| 4.1 | ⬜ | `cardStyle` скопирован 8 раз символ-в-символ — место в `theme`/`Card variant` | AddEntryPage, RecipeDetailPage, RecipeEditorPage, RecipesPage, WeightHistoryPage, workoutShared, mealPlan/types, measurements/shared |
| 4.2 | ⬜ | Фон страницы — копипаст в 17 файлах; хелпер `workoutPageBackground` уже есть | все страницы |
| 4.3 | ⬜ | 4 разных `formatDateRu` + 5-й через `Intl` — юзер видит 4 формата даты; `formatDate(ISO)` продублирован 8 раз | WeightHistoryPage:49, measurements/shared:26, mealPlan/types:67, DayChanger:34, reports/utils:40 |
| 4.4 | ⬜ | ~500 русских строк захардкожены мимо `i18n/ru.ts` (MealPlanPage — вообще без `t()`) | AddEntryPage (34), TodayPage (29), MealPlanPage (19), EntryPage, admin-табы |
| 4.5 | ⬜ | Эмодзи в разметке при готовом `ui/Icon.tsx` | MealPlanPage, ProductsPage:117, MeasurementHistoryList:26, PlanMealCard, TemplateCard:129 |
| 4.6 | ⬜ | `useDebounce` переопределён локально при готовом хуке | `pages/ProductsPage.tsx:25` |
| 4.7 | ⬜ | `MenuTile` объявлен внутри рендера Drawer — пересоздание поддерева каждый рендер | `widget/Drawer.tsx:67` |
| 4.8 | ⬜ | Мёртвый стейт `setPrediction` + лишний запрос `/weight/prediction` при каждом заходе в профиль | `pages/ProfilePage.tsx:30` |

## Блок 5 — Архитектура / производительность

| # | Статус | Проблема | Где |
|---|--------|----------|-----|
| 5.1 | ⬜ | Нет code-splitting: 27 страниц в одном бандле (admin грузится всем) — `React.lazy` | `App.tsx` |
| 5.2 | ⬜ | Страницы-гиганты: RecipeEditorPage 961, TodayPage 834, AddEntryPage 722 — вынести в `widgets/*` по FSD | pages/ |
| 5.3 | ⬜ | N+1 агрегаций при завершении тренировки (по одной на упражнение); нет индекса `{userId, exerciseId}` | `workouts/workout.service.ts:697` |
| 5.4 | ⬜ | Лидерборд: `find({weekKey}).sort({xpWeek})` без индекса — COLLSCAN; приватные фильтруются после `.limit(50)` | `social/social.controller.ts:72`, `user-stats.schema.ts` |
| 5.5 | ⬜ | Генерация плана питания грузит все рецепты+300 продуктов в память на каждый запрос | `meal-plan/meal-plan.service.ts:89-164` |
| 5.6 | ⬜ | Поиск по неякорному `$regex` мимо индекса + перевыборка `limit*3` с сортировкой в JS | `products.service.ts:33`, recipes.service |
| 5.7 | ⬜ | `GET /water` без даты отдаёт всю историю; `?limit=abc` → `.limit(NaN)` в ряде эндпоинтов | water, weight, friends, measurements |
| 5.8 | ⬜ | Пагинация: `GET /feed` (жёсткие 50), вес (90), замеры (90) — старое недоступно; `usePaginatedList` подключён только в рецептах | api + web |
| 5.9 | ⬜ | Несогласованные ответы API: `{ok:true}` vs объект vs `{found:false}` с 200; `GET /products/:id` отдаёт 200 с пустым телом вместо 404 | все контроллеры |
| 5.10 | ⬜ | Ошибки через `new Error(...)` → 500 вместо 400/404 | profile.service:32, social.controller:26, friends.service:75 |

---

*Найдено автоматическим аудитом + ручным проходом. Детали и точные строки — в истории сессии Claude Code.*
